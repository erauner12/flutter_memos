module flutter_memos/mcp_tcp_proxy_go

go 1.21 // Or a newer stable version like 1.22

// No external dependencies needed for basic TCP/Process handling yet
// Add dependencies here if needed later (e.g., for more robust JSON handling or logging)